import React from "react";
import styled from "styled-components";

const IconBottomFooter = ({ to, src, alt }) => {
  return (
    <Link to="twitter">
      <Image />
    </Link>
  );
};

export default IconBottomFooter;
